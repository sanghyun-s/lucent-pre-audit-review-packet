# Core data + ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# API server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # required by FastAPI for File/Form uploads

# Phase 4 (GPT narrative layer) — uncomment when implementing
# openai>=1.30
# anthropic>=0.40

# Phase 5 Tier 2 (Excel export) — uncomment when adding the .xlsx download
# openpyxl>=3.1
httpx>=0.27   # required by FastAPI TestClient for api_test.py
openai==2.40.0
python-dotenv==1.2.2
